import 'package:flutter/material.dart';

class NotiTestScreen extends StatelessWidget {
  const NotiTestScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Noti Test Screen'),
      ),
    );
  }
}
